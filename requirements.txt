# six: required by python-dateutil (pandas); pin recent version — very old six
# (e.g. 1.11) can break `from six.moves import _thread` on modern Python.
six>=1.16.0
pandas>=2.0
openpyxl>=3.1
# .xls (Excel 97–2003): pandas needs xlrd ≥2.0.1 (e.g. rent mapping table_zones *.xls).
xlrd>=2.0.1
geopandas>=0.14
# Parquet: thermal_comfort, sale_price, rent gold use GeoDataFrame.to_parquet(engine="pyarrow")
pyarrow>=14
shapely>=2.0
gdown>=5.0

# Sale price PDF → silver (src/silver/sale_price_median_to_silver.py)
pdfplumber>=0.11

# Database
sqlalchemy>=2.0
pymysql>=1.1
python-dotenv>=1.0

# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Tests
pytest>=8.0
httpx>=0.27
